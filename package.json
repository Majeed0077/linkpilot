{
  "name": "linkpilot",
  "version": "1.0.0",
  "private": true,
  "description": "A production-ready URL shortener with a Next.js frontend and Express API.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green --kill-others-on-fail \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "overrides": {
    "postcss": "^8.5.15",
    "next@16.2.9": {
      "postcss": "^8.5.15"
    }
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
